# Examples

A LangChain instrumentation example is being added in the next commit.

The example will show how to wrap a LangChain agent so its actions emit the 9 attributes defined in this proposal into your existing OpenTelemetry pipeline.

Check back shortly.
